name: eslint-check
on: push
jobs:
    my-job:
        runs-on: ubuntu-latest
        steps:
            with:
                node-version: '14'
            - name: Run linter
            - run: make lint

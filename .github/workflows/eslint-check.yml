name: eslint-check
on: push
jobs:
    my-job:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/setup-node@v2
            with:
                node-version: '14'
            - name: Run linter
            - run: make lint
